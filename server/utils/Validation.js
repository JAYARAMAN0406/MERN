const FIRST_NAME_REUIRED="Firstname is required";

const FIRST_NAME_MIN="First name must be at least 2 characters";

const LAST_NAME_REUIRED="Lastname is required";

const LAST_NAME_MIN="Last name must be at least 2 characters";

const EMAIL_REUIRED="Email is required";

const EMAIL_VALID="Please provide a valid email address";

const PASSWORD_REUIRED="Password is required";

const PASSWORD_CHAR="Password must be 8–16 chars, include uppercase, lowercase, number & special char";

const PHONE_REUIRED="Phone is required";

const PHONE_VALID="Phone number must be 10 digits";

const DOB_REUIRED="Date of birth is required";

const GENDER_REQUIRED="Gender is required";

const EMPLOYEE_VALIDATE="Employee validation";

const USER_VALIDATION="User Validation"



module.exports={
    FIRST_NAME_REUIRED,
    FIRST_NAME_MIN,
    LAST_NAME_REUIRED,
    LAST_NAME_MIN,
    EMAIL_REUIRED,
    EMAIL_VALID,
    PASSWORD_REUIRED,
    PASSWORD_CHAR,
    PHONE_REUIRED,
    PHONE_VALID,
    DOB_REUIRED,
    GENDER_REQUIRED,
    EMPLOYEE_VALIDATE,
    USER_VALIDATION
}